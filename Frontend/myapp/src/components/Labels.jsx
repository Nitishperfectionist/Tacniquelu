import React from 'react'

const Labels = ({text}) => {
  return (
    <label className="block mb-2 text-sm text-left font-medium text-gray-900 dark:text-black">
        {text}
    </label>
  )
}

export default Labels
